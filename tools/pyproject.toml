[project]
name = "htb-tools"
version = "0.1.0"
description = "Operator scripts for hack-the-body — read/manage state without going through the FE."
requires-python = ">=3.12"
dependencies = [
  "httpx>=0.27",
  "python-dotenv>=1.0",
]

[tool.ruff]
line-length = 100
